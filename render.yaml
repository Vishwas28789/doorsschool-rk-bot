services:
  - type: web
    name: doers-school-rk-bot
    env: node
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: GROQ_API_KEY
        sync: false
      - key: WEBINAR_LINK
        sync: false
      - key: NODE_ENV
        value: production
